Allow setting new categorization templates
	Make Category a textbox (dropdown later)
	Track what changes were made
	Save the changes as new Templates (just use the full info, don't try to be clever)
	
Switch to using a dropdown for Category

Make Category font match other cells (should also help with sizing)
	unless OBE

Some system for creating generic templates